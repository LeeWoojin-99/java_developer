package Bank;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.text.DecimalFormat;

/*
은행 업무 프로그램
1. 계좌번호는 자동 생성 "Bank-0001"
발행 순서에 따라 0001부터 순차적으로 증가
2. 소유주, 초기 입금 금액은 입력받기
3. 같은 소유주가 계좌 번호를 여러 개를 생성할 수 있다.
4. 하나의 계좌에 대해서 입금과 출금이 되도록
5. 입금과 출금이 이루어지면 남아있는 잔액 출력
6. 잔액이 부족하면 출금시 잔액부족을 출력
7. 없는 계좌 오류


*/

public class App {
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		String input;
		Management MG = new Management();
		

		do {
			MG.printMenu();
			input = br.readLine();
			
//			System.out.println(String.format(new DecimalFormat("0000").format(Integer.parseInt(input))));
			
			switch(input) {
			case "1": MG.createAccount(br); break;
			case "2": MG.deposit(br); break;
			case "3": MG.withdraw(br); break;
			case "4": MG.printAccountList(); break;
			case "q": System.out.println("종료"); break;
			default:  break;
			}
		}while(!input.equals("q"));
	}
}
