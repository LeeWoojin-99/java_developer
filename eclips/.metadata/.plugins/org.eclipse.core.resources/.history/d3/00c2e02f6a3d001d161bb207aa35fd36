package Lotto;

import java.util.Scanner;

public class Management {
	Lotto lottoUser;
	
	public void createLotto(Scanner sc) throws Exception{
		String inputStrArr[];
		int inputIntArr[] = new int[inputStrArr.length];
		
		System.out.println("로또 번호를 입력해주세요.");
		inputStrArr = sc.nextLine().split(" ");
		
		if(inputStrArr.length < 6 || inputStrArr.length > 7) {
			throw new RuntimeException("올바르지 않은 입력입니다.");
		}
		
		for(int i=0; i<inputStrArr.length; i++) {
			inputIntArr[i] = Integer.parseInt(inputStrArr[i]);
		}
		
//		for(int i: inputIntArr) System.out.print(i+" "); // test code
	}
	
	public void createLottoAuto() {
		
		System.out.println("로또 번호 생성이 완료되었습니다.");
	}
}
